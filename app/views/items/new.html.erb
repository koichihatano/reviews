<h1>ご購入商品の情報を入力</h1>
<%= form_with model: @item, local: true do |form| %>
  <p><%= form.label :name %> <%= form.text_field :name %></p>
  <p><%= form.label :price %> <%= form.number_field :price %></p>
  <%= form.submit %>
<% end %>