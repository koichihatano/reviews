<div>
  <p>【メールアドレス】 <%= post.user.email %></p>
  <p>【内容】 <%= post.content %></p>
  <p>
    <%= link_to "詳細", post %>
    <% if current_user.id == post.user_id %>
      <%= link_to "削除", post_path(post), data: { turbo_method: :delete, turbo_confirm: "削除してよろしいですか？" } %>
      <%= render "like", post: post, is_liked: post.is_liked %>  
  </p>
</div>
<hr>
